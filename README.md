# SanVIST 运营后台原型

独立的社区运营后台原型，包含话题管理、内容管理和内容审核。

## 本地运行

```bash
npm install
npm run dev
```

默认本地地址由 Vite 输出；本工作区固定使用 `http://localhost:5175/`。

## 发布地址

`https://huajie-git.github.io/SANY/admin/`

本项目使用 `admin` 分支发布到 GitHub Pages 的 `admin` 子目录。
